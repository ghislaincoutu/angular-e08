import { Highlight01Directive } from './highlight01.directive';

describe('Highlight01Directive', () => {
  it('should create an instance', () => {
    const directive = new Highlight01Directive();
    expect(directive).toBeTruthy();
  });
});
