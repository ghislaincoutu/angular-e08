import { Transform03Pipe } from './transform03.pipe';

describe('Transform03Pipe', () => {
  it('create an instance', () => {
    const pipe = new Transform03Pipe();
    expect(pipe).toBeTruthy();
  });
});
